name: Display File Content on PR Creation

on:
  pull_request:
    types: [opened]

jobs:
  display-file-content:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Display File Content
      run: |
        # Extract file URL from PR body
        FILE_URL=$(jq -r '.pull_request.body' ${{ github.event_path }} | grep -oP 'https?://\S+')
        
        # Download file content
        FILE_CONTENT=$(curl -s $FILE_URL)
        
        # Display file content
        # Display file content
        echo "Content of new.yaml:"
        cat new.yaml
